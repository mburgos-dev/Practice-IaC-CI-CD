---
- name: Configurar Nginx con Docker Compose
  hosts: local
  tasks:
    - name: Levantar Nginx con Docker Compose
      command: docker-compose up -d
      args:
        chdir: "{{ playbook_dir }}/../docker"
